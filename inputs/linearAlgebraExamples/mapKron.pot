import LinearAlgebra
import Bool
import Nat

main = map is_z_nut to_nat (kron is_z_bool op m1 m2)
;

is_z_nut x = eqNat x (Zero)
;

is_z_bool x = eqBool x (False)
;

op x y = and x y
;

to_nat x = (Succ(Zero))
